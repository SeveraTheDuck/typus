# Codecov configuration. Coverage is informational only -- it never fails a PR
# status check (no target threshold gate), per project policy.
coverage:
  status:
    project:
      default:
        informational: true
    patch:
      default:
        informational: true
comment: false
