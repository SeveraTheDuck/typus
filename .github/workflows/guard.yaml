name: Guard
on:
  pull_request:
    branches: [main]
permissions:
  contents: read
jobs:
  source-branch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: ./.github/actions/guard-source-branch
